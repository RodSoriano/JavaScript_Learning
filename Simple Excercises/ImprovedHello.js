// Write a program that asks the user for his first name and last name. 
// It then concatenates them and displays them in a single sentence.

//console.log("Hello World");

const fisrtName = prompt("Enter Your First Name:");
const lastName = prompt("Enter Your Last Name:");

let hello = `Hello, ${fisrtName} ${lastName}`;

console.log(hello);